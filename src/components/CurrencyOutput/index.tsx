import React from 'react';

const CurrencyOutput: React.FC = () => {
    return (
        <div className="p-2 bg-gray-700 text-white">
            {/* Display the conversion result here */}
        </div>
    );
};

export default CurrencyOutput;
